[package]
name = "astroy"
version = "0.1.0"
edition = "2021"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3

[dependencies]
bevy = { version = "0.15.0", features = ["dynamic_linking"] }
bevy_aseprite_ultra = "0.3.0"
